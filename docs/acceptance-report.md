# Acceptance report — Juiced Dashboard view migration

Automated parity of every committed view (`dashboard/views/*.yaml`) against the live default
dashboard, generated by `scripts/acceptance_suite.py`: each committed view is re-rendered with
a mapping regenerated from the default export and deep-diffed against the default view. This is
the M8 acceptance gate (roadmap PR-41).

## Summary

- **27 / 27 views migrated** and wired into `dashboard/dashboard.yaml` (composition order == default view order).
- **18 views are byte-identical** to the default (0-diff round-trip).
- **8 views carry only documented deliberate diffs** (privacy labels / stable-path / T7 untangle).
- **1 view (inkomhal)** uses hand-curated logical keys (PR-08); the auto-regenerated mapping cannot render it here, so its parity was verified structurally at migration time (identical card tree, only placeholder labels differ).
- Whole `dashboard/` tree is privacy-clean (no household names; enforced by CI `check_entity_refs` + `grep`).

## Byte-identical views (0 diffs)

| # | view |
| --- | --- |
| 1 | oprit |
| 3 | toilet |
| 4 | woonkamer |
| 5 | keuken |
| 6 | garage |
| 8 | serverroom |
| 11 | badkamer |
| 13 | terras |
| 15 | net |
| 16 | valliant |
| 18 | water |
| 20 | car |
| 21 | energy |
| 22 | batteries1 |
| 23 | ecopower |
| 24 | anycubic |
| 25 | zwembad |
| 26 | garden |

## Views with documented deliberate diffs

| # | view | diffs | reason |
| --- | --- | --- | --- |
| 0 | home | 4 | T7 untangle (explicit type: sections, stable path: home, dropped empty top-level cards[]) + privacy label (Kamer Adriaan -> Kinderkamer) |
| 7 | bureau | 1 | privacy label: "Bureau Joost" -> "Bureau Persoon 1" |
| 9 | slaapkamer | 2 | privacy labels: "Joost"/"Leen" -> "Persoon 1"/"Persoon 2" |
| 10 | kinderkamer | 1 | privacy label: motion state-text genericised (minor's room) |
| 12 | logeerkamer | 1 | T4 stable path: logeerkamer added (default view had none) |
| 14 | house | 1 | privacy label: "Kamer Adriaan" -> "Kinderkamer" |
| 17 | hainfo | 2 | privacy labels: "5g data Joost/Leen" -> "5g data Persoon 1/2" |
| 19 | person | 3 | privacy labels: "Devices/Objecten Joost/Leen" -> "... Persoon 1/2" |

### Special case — inkomhal

- **inkomhal** (index 2): committed keys were hand-curated to readable names in PR-08, so the
  auto-regenerated (default-derived) mapping does not cover them. Parity was verified at
  migration via re-extraction: the committed card tree is structurally identical to the
  default (0 structural diffs); only the placeholder *labels* differ.

## Staging + frontend verification

Every view was staged surgically to `mcp-test-dashboard` at its default index (snapshot taken;
the other 26 views byte-identical after each write; staged view == committed render) and
rendered in headless Chromium (desktop 1600x1200 + mobile 400x900). All views render with
**0 unresolved `<<placeholder>>`s**.

Three views render pre-existing `hui-error-card`s that are **not regressions** — the default
dashboard shows the same counts (custom cards whose HACS resources are not loaded on this
instance); the migrations are byte-identical to the default:

| view | error-cards (committed) | error-cards (default) |
| --- | --- | --- |
| terras | 1 | 1 |
| net | 2 | 2 |
| person | 1 | 1 |

## Responsive / performance

- Each view verified at desktop (1600x1200) and mobile (400x900); conditional / restriction /
  expander cards render per their conditions, no layout errors.
- A full runtime performance re-baseline against `docs/performance-baseline.md` (PR-06) is a
  separate measurement step, best run on the assembled staging dashboard (PR-42) since it needs
  the deployed yaml-mode dashboard rendered with the real entity mapping.

## Not covered here (human-gated)

- **PR-42** staging dashboard `juiced-home`: registering a new yaml-mode dashboard on the HA
  host (edit `configuration.yaml` + render with the real `entities.local.yaml`) is a host-level
  change, gated on explicit approval.
- **Cutover** of the default dashboard: a separate, explicitly-approved step; the default stays
  untouched until then.
