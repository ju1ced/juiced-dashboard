{
  "name": "juiced-dashboard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "HACS-installable custom cards for the Juiced dashboard, alongside its YAML dashboard config and dev tooling (markdown lint).",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "lint:md": "markdownlint-cli2 \"**/*.md\"",
    "build": "node scripts/build.mjs",
    "typecheck": "tsc --noEmit",
    "check:syntax": "node --check dist/juiced-dashboard.js",
    "pretest": "npm run build",
    "test": "node --test",
    "verify": "npm run typecheck && npm run build && npm run check:syntax && node --test"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "markdownlint-cli2": "^0.23.0",
    "typescript": "^5.7.2"
  }
}
