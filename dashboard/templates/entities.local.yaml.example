# Copy this file to `entities.local.yaml` (git-ignored) and fill in your REAL entity IDs.
#
#   cp dashboard/templates/entities.local.yaml.example dashboard/templates/entities.local.yaml
#   python scripts/init_mapping.py     # adds a TODO line for every placeholder in use
#
# Then replace each `TODO: set entity_id` with your real entity ID. See docs/entity-mapping.md.
# Structure follows entities.example.yaml. Real IDs stay only on your machine.

nav:
  home_button: "TODO: set entity_id"
  kiosk_toggle: "TODO: set entity_id"

energy:
  battery_soc: "TODO: set entity_id"
  charge_power: "TODO: set entity_id"
  discharge_power: "TODO: set entity_id"
  solar_power: "TODO: set entity_id"
  grid_import: "TODO: set entity_id"
  peak_power: "TODO: set entity_id"
  charger_daily: "TODO: set entity_id"
